import { ReplaceVowelsForNumbersPipe } from './replace-vowels-for-numbers.pipe';

describe('ReplaceVowelsForNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceVowelsForNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
